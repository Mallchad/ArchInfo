# ip link
# wifi-menu <network_interface>
# lsblk
# cfdisk /dev/<device>
~ create boot partition
~ create swap partition
~ create root partition
# mkfs.ext4 /dev/<root_partition>
# mkswap /dev/<swap_partition>
# mkfs.fat /dev/<boot_partition>
# mount /dev/<root_partition> /mnt
# mount /dev/<boot_partition> /mnt/boot
# swapon /dev/<swap_partition>
# <text_editor> /etc/pacman.d/mirrorlist
# genfstab -U /mnt >> /mnt/etc/fstab
# arch-chroot /mnt
# pacman -S <text_editor>
# lf -sf /usr/share/zoneinfo/<region>/<city> /etc/localtime
# hwclock --systohc
# <text_editor> /etc/locale.gen
~ uncomment <chosen_locale>
# LANG=en_US.UTF-8 > /etc/locale.confg
# <hostname > /etc/hostname
# passwd
> <root_password>
# pacman -S grub
//for BIOS
# grub-install --target=i386-pc
//for UEFI
# grub -S efibootmgr
# grub-install --target=x86-efi --efi-directory=/boot --bootloader-id=<bootloader_name>
# grub-mkconfig -o /boot/grub/grub.cfg

# useradd -m -G <additional_groups> -S /bin/bash <username>
# pacman -S xorg
# pacman -S <video_drivers>
# pacman -S xorg-xinit
# cp /etc/X11/xinit/xinitrc ~/.xinitrc
# pacman -S <plasma-version>
# pacman -S <kde-applications_version>
# exec startkde >> ~/.xinitrc
# startx